# Local dev
/.deployment-credentials.php
/.docker/
/.backups/
/.data/
/web/.htdeployment
/web/www/info.php

# Composer
/web/vendor/

# Frontend (npm / Vite) – tooling lives in repo root, not in web/ (not uploaded to hosting)
/node_modules/

# Nette
/web/temp/
/web/log/
/web/data/

# Nette Tester failure artifacts (*.actual / *.expected of a failed assertion)
/web/tests/**/output/

# Local config overrides (secrets, per-environment)
/web/config/local.neon
