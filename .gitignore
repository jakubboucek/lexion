# Local dev
/.docker/

# Composer
/web/vendor/

# Frontend (npm / Vite) – tooling lives in repo root, not in web/ (not uploaded to hosting)
/node_modules/

# Nette
/web/temp/
/web/log/
/web/data/

# Local config overrides (secrets, per-environment)
/web/config/local.neon
