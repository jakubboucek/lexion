{varType Nette\Application\UI\Form $form}
{varType Nette\Database\Table\ActiveRow $group}

{block title}Přejmenovat skupinu{/block}

{block breadcrumbs}
<li><a n:href="default">Přehled</a></li>
<li>Přejmenovat skupinu</li>
{/block}

{block content}
<div class="mx-auto mt-8 w-full max-w-md">
    <div class="card border border-base-300 bg-base-100 shadow-sm">
        <div class="card-body">
            <h1 class="card-title">Skupina „{$group->name}“</h1>

            <form n:name="groupEditForm" class="mt-2 flex flex-col gap-4">
                <div n:foreach="$form->getOwnErrors() as $error" class="alert alert-error text-sm">
                    {$error}
                </div>

                <fieldset class="fieldset w-full">
                    <label n:name="name" class="font-semibold">Název skupiny</label>
                    <input n:name="name" autofocus class="input w-full">
                    <p n:foreach="$form['name']->getErrors() as $error" class="text-sm text-error">{$error}</p>
                </fieldset>

                <button n:name="send" class="btn btn-primary mt-2">Uložit</button>
            </form>
        </div>
    </div>

    <p class="mt-4 text-center text-sm">
        <a n:href="default" class="link link-hover">← Zpět na přehled</a>
    </p>
</div>
