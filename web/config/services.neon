services:
  - App\Core\Authenticator
  router: App\Core\RouterFactory::createRouter()


search:
  - in: %appDir%
    classes:
      - *Facade
      - *Factory
      - *Repository
      - *Service
      - *Client
